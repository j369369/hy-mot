# 이재원 - 2023161840

# 1. Come up with a function that divides the first input by the second input:
def div(a, b):
    return a / b

# 1-1. example
print(div(4, 2))
# Answer = 2.0

# Use the con function for the following question
def con(a, b):
    return(a + b) 

# 2. can the con function we defined before be used to add two integers or strings?
print(con(1, 2)) # integer
print(con("a", "b")) # string
# Answer = 3, ab

# 3. Can the con function we defined before be used to concatenate two lists or tuples?
print(con([1, 'a'], [3, 'b'])) # list
print(con(('c', 2), ('d', 4))) # tuple
# Answer = [1, 'a', 3, 'b'], ('c', 2, 'd', 4)

